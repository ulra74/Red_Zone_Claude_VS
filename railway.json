{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate --settings=red_zone_academy.settings_production && python manage.py collectstatic --noinput --settings=red_zone_academy.settings_production && gunicorn red_zone_academy.wsgi:application --bind 0.0.0.0:$PORT --settings=red_zone_academy.settings_production",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}